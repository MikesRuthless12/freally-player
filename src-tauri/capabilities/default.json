{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main player window: the default Tauri core permissions (events, window state, app metadata), process exit for the first-run EULA gate's 'Decline & Quit', and the open-file dialog so the user can pick media. Nothing more — broader permissions are added per feature, never wholesale.",
  "windows": ["main"],
  "permissions": ["core:default", "process:allow-exit", "dialog:allow-open"]
}
