//! `freally-convert` — owned conversion/clip/record preset and job orchestration over the
//! ffmpeg engine.
//!
//! Stub crate: conversion lands in Phase 9 (see `product-roadmap.md`). The ffmpeg engine
//! itself is non-owned and sits behind the `Engine` boundary in `freally-player-core`.

#![forbid(unsafe_code)]
