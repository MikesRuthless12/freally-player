//! `freally-library` — owned media library: folder scan, opt-in scraping, watch-state, schema.
//!
//! Stub crate: the library lands in Phase 7 (see `product-roadmap.md`). Opt-in network
//! (TMDB/TVDB/MusicBrainz) is the only outbound use here, and only when the user enables it.

#![forbid(unsafe_code)]
