# freally-library — owned media library: folder scan, opt-in scraping, watch-state, schema.
# 100% owned; opt-in network (TMDB/TVDB/MusicBrainz) is the only outbound use, and only when enabled.
[package]
name = "freally-library"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
rusqlite.workspace = true     # local library / watch-state / playlists / scrape cache
serde.workspace = true
serde_json.workspace = true
directories.workspace = true
log.workspace = true
